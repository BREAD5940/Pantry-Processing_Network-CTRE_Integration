language: java
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
install: git clone https://github.com/BREAD5940/Pantry-Logging && git clone https://github.com/BREAD5940/Pantry-Processing_Network
script: ./gradlew test --include-build "./Pantry-Logging" --include-build "./Pantry-Processing_Network"
after_script: echo hello
